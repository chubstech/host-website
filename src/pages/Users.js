import React from 'react';

const Users = () => {
    return (
       <div>
          <h1>Users</h1>
          <p>Security wise, I do not think it is a great idea to list all employees here. The only thing i think this page
             should do is only allow the admin to assign an employee name/id to a section of our device
          </p>
       </div>
    );
}

export default Users;
